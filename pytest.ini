[pytest]
; Root CHỈ chạy test tầng nền. Mỗi app chạy pytest RIÊNG từ thư mục app
; (tools/scripts/test-all.ps1) — 2 app cùng tên package `src` không đụng nhau
; vì mỗi app một process (Luật 2: app tự đủ).
testpaths = tests
