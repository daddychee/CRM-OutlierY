"""Radary — radar video outlier đa niche (Phase 1: core engine + SQLite multi-tenant).

Luật nghiệp vụ theo docs/radar_spec.md v3. Chỉ dùng stdlib (Python 3.9+).
Production cũ (daily_radar.py + radar_state/) KHÔNG bị đụng — package này ghi vào data/.
"""
__version__ = '0.1.0'
