# migrations app tri-thuc

Chua co migration nao — store hien tai la file JSON/CSV ghi nguyen tu (khong SQLite).
Khi them DB co schema thi danh so migration o day + bang schema_version (hien phap muc 2).
