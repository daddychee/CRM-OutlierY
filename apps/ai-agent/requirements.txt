# App tri-thuc — phụ thuộc RIÊNG của app (Luật 2: app tự đủ).
# Giai đoạn test dùng venv chung ở ROOT; khi tách venv riêng thì cài từ file này.

# --- lõi web + test (bắt buộc, cả khi chạy mock) ---
fastapi
uvicorn[standard]
jinja2
python-multipart
httpx
python-dotenv
pypdf
qdrant-client
requests
youtube-transcript-api
python-docx        # đọc .docx khi nạp; test_remake_dep dựng fixture docx
openai             # provider openai_compatible (GLM/ChatGPT/DeepSeek/Grok); test ghim timeout/retry
openpyxl           # đọc .xlsx khi nạp tài liệu
pytest

# --- chạy THẬT mới cần (mock không đụng tới; import lazy trong module) ---
# fastembed          # embedding local — nặng; test tương ứng tự skip khi thiếu
# anthropic          # provider anthropic (WRITER_PROVIDER=anthropic)
# weasyprint         # bản đẹp PDF (cần GTK trên Windows — hệ cũ ghi nhận fail baseline;
#                    # 3 test bản đẹp tự skip khi thiếu)
